// import { RECEIVE_TODOS } from "../actions/todo_actions";


export const todosReducer = (state = {}, action) => {
    
    switch (action.type) {
        // case RECEIVE_TODOS: 
        //     return {} 
    default: 
        return state;
    }

}